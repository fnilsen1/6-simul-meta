# Describing a solution given a pin order

Use `generate_6simul_data.py` to generate a dict containing information about how to solve
using that pin order.

It contains:
- conditions
- memo
- intuitive/memorized moves (which depends on the order of execution of UP/DOWN pins!)
- blocks that the intiotive moves much match
